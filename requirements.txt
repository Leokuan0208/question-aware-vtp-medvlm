# Python packages required to build and preview the site locally.
# Install all of them at once with:
#   pip install -r requirements.txt

mkdocs==1.6.1                 # the static-site generator itself
mkdocs-material==9.5.49       # the Material theme (look + features)
pymdown-extensions==10.12     # extra markdown features (admonitions, tabs, etc.)
mkdocs-git-revision-date-localized-plugin==1.3.0  # shows "last updated" on each page
mkdocs-print-site-plugin      # generates single-file HTML file